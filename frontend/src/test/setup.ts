/**
 * Global test setup — runs before every test file.
 *
 * Imports jest-dom so all @testing-library/jest-dom matchers
 * (toBeInTheDocument, toHaveTextContent, etc.) are available globally.
 */
import "@testing-library/jest-dom";
